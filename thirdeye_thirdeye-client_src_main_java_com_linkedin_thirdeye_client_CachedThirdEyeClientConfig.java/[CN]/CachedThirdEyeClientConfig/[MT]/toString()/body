{
  return Objects.toStringHelper(CachedThirdEyeClientConfig.class).add("expireAfterAccess",expireAfterAccess).add("expirationTime",expirationTime).add("expirationUnit",expirationUnit).add("useCacheForExecuteMethod",useCacheForExecuteMethod).toString();
}
