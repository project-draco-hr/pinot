{
  int metricSum=0;
  for (  StarTreeRecord record : new StarTreeRecordStreamAvroFileImpl(avroFile,Arrays.asList("A","B","C"),Arrays.asList("M"),"hoursSinceEpoch")) {
    metricSum+=record.getMetricValues().get("M");
  }
  Assert.assertEquals(metricSum,100);
}
