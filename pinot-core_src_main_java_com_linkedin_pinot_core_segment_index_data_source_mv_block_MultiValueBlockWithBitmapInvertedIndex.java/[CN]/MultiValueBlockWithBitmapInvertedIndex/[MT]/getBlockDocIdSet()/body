{
  if (predicate == null) {
    return BlockUtils.getDummyBlockDocIdSet(columnMetadata.getTotalDocs());
  }
  final ImmutableRoaringBitmap b=DictionaryIdFilterUtils.filter2(predicate,invertedIndex,dictionary,columnMetadata);
  return BlockUtils.getBLockDocIdSetBackedByBitmap(b);
}
