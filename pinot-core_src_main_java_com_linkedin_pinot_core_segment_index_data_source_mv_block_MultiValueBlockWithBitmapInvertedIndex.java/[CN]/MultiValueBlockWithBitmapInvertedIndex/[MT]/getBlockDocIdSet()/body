{
  if (filteredIds == null) {
    return BlockUtils.getDummyBlockDocIdSet(columnMetadata.getTotalDocs());
  }
  return BlockUtils.getBLockDocIdSetBackedByBitmap(DictionaryIdFilterUtils.filter2(predicate,invertedIndex,dictionary,columnMetadata));
}
