{
  if (a == null) {
    return b;
  }
 else   if (b == null) {
    return a;
  }
  Number[] result=new Number[a.length];
  for (int i=0; i < a.length; i++) {
    result[i]=a[i].doubleValue() + b[i].doubleValue();
  }
  return result;
}
