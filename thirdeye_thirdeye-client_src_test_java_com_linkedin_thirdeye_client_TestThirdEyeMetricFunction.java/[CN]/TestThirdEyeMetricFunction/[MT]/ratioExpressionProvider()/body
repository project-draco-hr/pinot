{
  List<String> metricsM1M2=Arrays.asList("m1","m2");
  String standard="RATIO(m1,m2)";
  String metricSpace1="RATIO(m1,m2)";
  String metricSpace2="RATIO(m1 ,m2)";
  String parenSpace="RATIO( m1,m2 )";
  String quotedMetrics="RATIO('m1','m2')";
  String lowercaseRatio="ratio(m1,m2)";
  String[] testStrs=new String[]{standard,metricSpace1,metricSpace2,parenSpace,quotedMetrics,lowercaseRatio};
  Object[][] result=new Object[testStrs.length][];
  for (int i=0; i < testStrs.length; i++) {
    String string=testStrs[i];
    result[i]=new Object[]{string,ThirdEyeMetricFunction.Expression.RATIO,metricsM1M2};
  }
  return result;
}
