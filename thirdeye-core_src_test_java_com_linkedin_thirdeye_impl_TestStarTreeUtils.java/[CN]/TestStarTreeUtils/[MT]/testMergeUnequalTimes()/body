{
  try {
    StarTreeRecordImpl.Builder builder=new StarTreeRecordImpl.Builder().setDimensionValue("A","A1").setDimensionValue("B","B1").setDimensionValue("C","C1").setMetricValue("M",100).setMetricType("M",MetricType.INT);
    StarTreeRecord r1=builder.setTime(100L).build();
    StarTreeRecord r2=builder.setTime(200L).build();
    StarTreeUtils.merge(Arrays.asList(r1,r2));
  }
 catch (  Exception e) {
  }
}
