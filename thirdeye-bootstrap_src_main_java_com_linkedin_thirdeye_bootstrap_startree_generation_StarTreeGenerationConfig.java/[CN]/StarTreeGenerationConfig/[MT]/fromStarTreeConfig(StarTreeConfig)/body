{
  return new StarTreeGenerationConfig(config.getCollection(),config.getDimensionNames(),config.getMetricNames(),config.getMetricTypes(),config.getSplit().getOrder(),config.getTime().getColumnName(),config.getSplit().getThreshold());
}
