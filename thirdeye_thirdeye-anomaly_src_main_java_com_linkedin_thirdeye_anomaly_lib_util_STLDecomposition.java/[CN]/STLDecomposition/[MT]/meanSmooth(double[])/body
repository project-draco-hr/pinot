{
  double[] smoothed=new double[series.length];
  double mean=0;
  for (  double d : series) {
    mean+=d / series.length;
  }
  for (int i=0; i < series.length; i++) {
    smoothed[i]=mean;
  }
  return smoothed;
}
