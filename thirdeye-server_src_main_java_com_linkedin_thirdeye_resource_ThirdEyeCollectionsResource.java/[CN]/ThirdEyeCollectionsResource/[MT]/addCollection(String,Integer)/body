{
  if (helixManager == null) {
    throw new NotFoundException();
  }
  helixManager.getClusterManagmentTool().addResource(helixManager.getClusterName(),collection,partitions,StateModelDefId.OnlineOffline.stringify());
  return Response.ok().build();
}
