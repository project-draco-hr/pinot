{
  try (FilePerIndexDirectory columnDirectory=new FilePerIndexDirectory(segmentDir,segmentMetadata,ReadMode.mmap);PinotDataBuffer buffer=columnDirectory.newDictionaryBuffer("column1",1024)){
  }
   try (FilePerIndexDirectory columnDirectory=new FilePerIndexDirectory(segmentDir,segmentMetadata,ReadMode.mmap);PinotDataBuffer repeatBuffer=columnDirectory.newDictionaryBuffer("column1",1024)){
  }
 }
