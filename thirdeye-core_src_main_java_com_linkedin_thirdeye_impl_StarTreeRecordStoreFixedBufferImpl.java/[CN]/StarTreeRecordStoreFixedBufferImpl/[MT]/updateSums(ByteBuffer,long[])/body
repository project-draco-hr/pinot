{
  buffer.getLong();
  for (  String dimensionName : dimensionNames) {
    buffer.getInt();
  }
  buffer.getLong();
  for (int i=0; i < metricNames.size(); i++) {
    sums[i]+=buffer.getLong();
  }
}
