{
  if (!(obj instanceof MetricFunction)) {
    return false;
  }
  MetricFunction that=(MetricFunction)obj;
  return Objects.equal(this.functionName,that.functionName) && Objects.equal(this.metricName,that.metricName);
}
