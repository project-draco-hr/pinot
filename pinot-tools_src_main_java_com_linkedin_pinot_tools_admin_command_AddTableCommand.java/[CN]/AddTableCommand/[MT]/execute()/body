{
  JsonNode node=new ObjectMapper().readTree(filePath);
  String res=AbstractBaseCommand.sendPostRequest(ControllerRequestURLBuilder.baseUrl(controllerAddress).forTableCreate(),node.toString());
  System.out.println(res);
  return true;
}
