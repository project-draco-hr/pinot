{
  final String filePath=TestChunkIndexCreationDriverImpl.class.getClassLoader().getResource(AVRO_DATA).getFile();
  final SegmentGeneratorConfig config=SegmentTestUtils.getSegmentGenSpecWithSchemAndProjectedColumns(new File(filePath),INDEX_DIR,"daysSinceEpoch",TimeUnit.DAYS,"mirror","mirror");
  config.setSegmentNamePostfix("1");
  config.setTimeColumnName("daysSinceEpoch");
  final SegmentIndexCreationDriver driver=SegmentCreationDriverFactory.get(null);
  driver.init(config);
  driver.build();
  return new File(INDEX_DIR,driver.getSegmentName()).getAbsolutePath();
}
