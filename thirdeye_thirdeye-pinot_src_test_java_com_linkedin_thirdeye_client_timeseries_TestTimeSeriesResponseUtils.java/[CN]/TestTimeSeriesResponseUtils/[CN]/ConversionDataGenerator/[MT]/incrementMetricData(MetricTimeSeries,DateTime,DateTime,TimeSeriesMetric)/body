{
  long timeWindow=start.getMillis();
  for (  TimeSeriesMetric metric : timeSeriesMetrics) {
    metricTimeSeries.increment(timeWindow,metric.getMetricFunction().toString(),metric.getValue());
  }
}
