{
  return MoreObjects.toStringHelper(this).add("id",getId()).add("name",name).add("metricIds",metricIds).add("dataset",getDataset()).add("filterClause",filterClause).add("groupBy",groupBy).add("anomalyFunctionIds",anomalyFunctionIds).toString();
}
