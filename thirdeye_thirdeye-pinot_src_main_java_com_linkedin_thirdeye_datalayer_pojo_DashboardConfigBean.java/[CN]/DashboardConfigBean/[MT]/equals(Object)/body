{
  if (!(o instanceof DashboardConfigBean)) {
    return false;
  }
  DashboardConfigBean dc=(DashboardConfigBean)o;
  return Objects.equals(getId(),dc.getId()) && Objects.equals(name,dc.getName()) && Objects.equals(metricIds,dc.getMetricIds())&& Objects.equals(dataset,dc.getDataset())&& Objects.equals(filterClause,dc.getFilterClause())&& Objects.equals(groupBy,dc.getGroupBy())&& Objects.equals(anomalyFunctionIds,dc.getAnomalyFunctionIds());
}
