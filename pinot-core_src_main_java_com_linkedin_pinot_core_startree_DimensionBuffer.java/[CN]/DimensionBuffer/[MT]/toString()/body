{
  StringBuilder sb=new StringBuilder("[");
  String delim="";
  for (int i=0; i < numDimensions; i++) {
    sb.append(delim).append(buffer.get(i));
    delim=", ";
  }
  sb.append("]");
  return sb.toString();
}
