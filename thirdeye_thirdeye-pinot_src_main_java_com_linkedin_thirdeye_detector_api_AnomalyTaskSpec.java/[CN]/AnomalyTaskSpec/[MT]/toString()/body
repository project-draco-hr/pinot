{
  return MoreObjects.toStringHelper(this).add("taskId",taskId).add("jobExecutionId",jobExecutionId).add("status",status).add("startTime",taskStartTime).add("endTime",taskEndTime).add("taskInfo",taskInfo).toString();
}
