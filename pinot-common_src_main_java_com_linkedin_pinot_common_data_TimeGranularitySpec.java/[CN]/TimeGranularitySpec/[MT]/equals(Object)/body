{
  if (!(object instanceof TimeGranularitySpec)) {
    return false;
  }
  TimeGranularitySpec spec=(TimeGranularitySpec)object;
  if (this.getName().equals(spec.getName()) && spec.getDataType() == this.getDataType() && this.getTimeType() == spec.getTimeType() && this.getTimeunitSize() == spec.getTimeunitSize()) {
    return true;
  }
  return false;
}
