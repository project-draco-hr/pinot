{
  if (!(object instanceof TimeGranularitySpec))   return false;
  TimeGranularitySpec spec=(TimeGranularitySpec)object;
  if (this.getColumnName().equals(spec.getColumnName()) && spec.getDataType() == this.getDataType() && this.getTimeType() == spec.getTimeType())   return true;
  return false;
}
