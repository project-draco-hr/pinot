{
  IndexMetadata indexMetadata=new IndexMetadata();
  indexMetadata.minDataTime=properties.getProperty(StarTreeMetadataProperties.MIN_DATA_TIME) == null ? null : Long.parseLong(properties.getProperty(StarTreeMetadataProperties.MIN_DATA_TIME));
  indexMetadata.maxDataTime=properties.getProperty(StarTreeMetadataProperties.MAX_DATA_TIME) == null ? null : Long.parseLong(properties.getProperty(StarTreeMetadataProperties.MAX_DATA_TIME));
  return indexMetadata;
}
