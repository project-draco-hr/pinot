{
  if (from.equals(SegmentVersion.v1) && to.equals(SegmentVersion.v2)) {
    return new SegmentFormatConverterV1ToV2();
  }
  throw new UnsupportedOperationException("Unable to find a convertor to convert segment from:" + from + " to:"+ to);
}
