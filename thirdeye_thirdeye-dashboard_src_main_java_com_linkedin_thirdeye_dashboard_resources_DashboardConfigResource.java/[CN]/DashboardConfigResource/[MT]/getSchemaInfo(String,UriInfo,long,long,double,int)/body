{
  Map<String,Object> schemaInfo=new HashMap<>();
  schemaInfo.putAll(getDimensionInfo(collection,uriInfo,baselineMillis,currentMillis,contributionThreshold,dimensionValuesLimit));
  schemaInfo.putAll(getMetricInfo(collection));
  return schemaInfo;
}
