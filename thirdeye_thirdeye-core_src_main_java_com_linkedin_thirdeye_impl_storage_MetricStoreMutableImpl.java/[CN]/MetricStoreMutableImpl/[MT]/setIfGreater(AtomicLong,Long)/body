{
  while (true) {
    Long current=target.get();
    if (current == -1 || value > current) {
      if (target.compareAndSet(current,value)) {
        return;
      }
    }
 else {
      return;
    }
  }
}
