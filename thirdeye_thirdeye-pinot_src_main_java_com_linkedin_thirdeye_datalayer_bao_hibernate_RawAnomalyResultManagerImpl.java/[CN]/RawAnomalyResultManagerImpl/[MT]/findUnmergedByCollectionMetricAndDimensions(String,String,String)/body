{
  return getEntityManager().createQuery(FIND_UNMERGED_BY_COLLECTION_METRIC_DIMENSION,entityClass).setParameter("collection",collection).setParameter("metric",metric).setParameter("dimensions",dimensions).setParameter("dataMissing",false).getResultList();
}
