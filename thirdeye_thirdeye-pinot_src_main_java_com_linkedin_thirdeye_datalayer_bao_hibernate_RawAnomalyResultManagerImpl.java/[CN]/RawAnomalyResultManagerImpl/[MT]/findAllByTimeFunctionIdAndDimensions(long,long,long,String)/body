{
  return getEntityManager().createQuery(FIND_BY_TIME_FUNCTION_ID_DIMENSIONS,entityClass).setParameter("startTime",startTime).setParameter("endTime",endTime).setParameter("functionId",functionId).setParameter("dimensions",dimensions).getResultList();
}
