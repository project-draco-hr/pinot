{
  StringRepresentation presentation=null;
  final String tableName=(String)getRequest().getAttributes().get("tableName");
  if (tableName == null) {
    return new StringRepresentation("tableName is not present");
  }
  IndexingConfig config=null;
  try {
    JSONObject o=new JSONObject(entity.getText().toString());
    config=AbstractTableConfig.loadIndexingConfig(new ObjectMapper().readTree(o.getJSONObject("tableIndexConfig").toString()));
  }
 catch (  final Exception e) {
  }
  return presentation;
}
