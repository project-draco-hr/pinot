{
  ThirdEyeRequest request=ThirdEyeRequestUtils.buildRequest(starTreeConfig.getCollection(),null,new HashMap<String,String>(),Collections.singletonList(dimensionKeyContributionMetric),ONE_HOURS,timeRange);
  Map<DimensionKey,MetricTimeSeries> dataset=thirdEyeClient.execute(request);
  if (dataset.size() != 1) {
    throw new IllegalStateException();
  }
  return dataset.entrySet().iterator().next();
}
