{
  config=AbstractTableConfig.init(entity.getText());
  manager.updateIndexingConfigFor(config.getTableName(),TableType.valueOf(config.getTableType()),config.getIndexingConfig());
  return new StringRepresentation("done");
}
