{
  cache=(Map<K,V>)Collections.synchronizedMap(new LinkedHashMap<K,V>(maxSize + 1,.75F,true){
    public boolean removeEldestEntry(    Map.Entry eldest){
      return size() > maxSize;
    }
  }
);
}
