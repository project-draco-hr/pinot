{
  LOG.info("Creating FlowControlledThirdEyeClient from {} with {} max parallel requests",client,maxParallelRequests);
  this.client=client;
  this.requestPermits=new Semaphore(maxParallelRequests);
}
