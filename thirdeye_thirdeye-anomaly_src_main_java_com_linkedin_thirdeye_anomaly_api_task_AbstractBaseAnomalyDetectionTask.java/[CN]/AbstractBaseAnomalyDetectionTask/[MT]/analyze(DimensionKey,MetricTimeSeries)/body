{
  List<AnomalyResult> anomalyResults=function.analyze(dimensionKey,metricTimeSeries,taskInfo.getTimeRange(),functionHistory.getHistoryForDimensionKey(dimensionKey));
  filterAnomalyResults(anomalyResults,taskInfo.getTimeRange());
  return anomalyResults;
}
