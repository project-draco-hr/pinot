{
  int rows=25000000;
  int columnSizeInBits=3;
  boolean isMMap=true;
  boolean hasNulls=false;
  FixedBitSingleValueReader reader=new FixedBitSingleValueReader(file,rows,columnSizeInBits,isMMap,hasNulls);
  int[] result2=new int[rows];
  for (int i=0; i < rows; i++) {
    result2[i]=reader.getInt(i);
  }
}
