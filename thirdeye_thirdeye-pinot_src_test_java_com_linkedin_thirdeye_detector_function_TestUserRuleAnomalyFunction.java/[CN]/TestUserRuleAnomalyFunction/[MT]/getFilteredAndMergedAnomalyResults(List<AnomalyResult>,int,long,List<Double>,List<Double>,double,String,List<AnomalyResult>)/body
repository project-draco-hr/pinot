{
  List<AnomalyResult> actualAnomalyResults=function.getFilteredAndMergedAnomalyResults(anomalyResults,minConsecutiveSize,bucketMillis,baselineValues,currentValues,threshold,baselineProp);
  Assert.assertEquals(actualAnomalyResults,expectedAnomalyResults);
}
