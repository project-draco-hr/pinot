{
  return MoreObjects.toStringHelper(this).add("collection",collection).add("fromAddress",fromAddress).add("toAddress",toAddress).add("cron",cron).add("smtpHost",smtpHost).add("smtpPort",smtpPort).add("smtpUser",smtpUser).add("windowSize",windowSize).add("windowUnit",windowUnit).toString();
}
