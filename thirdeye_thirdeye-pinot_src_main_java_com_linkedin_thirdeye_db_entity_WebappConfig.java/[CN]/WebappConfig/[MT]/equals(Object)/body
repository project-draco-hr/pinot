{
  if (!(o instanceof WebappConfig)) {
    return false;
  }
  WebappConfig wc=(WebappConfig)o;
  return Objects.equals(getId(),wc.getId()) && Objects.equals(configId,wc.getConfigId()) && Objects.equals(collection,wc.getCollection())&& Objects.equals(configType,wc.getConfigType())&& Objects.equals(config,wc.getConfig());
}
