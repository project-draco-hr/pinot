{
  if (!(o instanceof WebappConfig)) {
    return false;
  }
  WebappConfig wc=(WebappConfig)o;
  return Objects.equals(getId(),wc.getId()) && Objects.equals(name,wc.getName()) && Objects.equals(collection,wc.getCollection())&& Objects.equals(type,wc.getType())&& Objects.equals(config,wc.getConfig());
}
