{
  ThirdEyeClientFactory factory=(ThirdEyeClientFactory)Class.forName(this.factoryClassName).newInstance();
  if (cacheConfig != null) {
    factory.configureCache(cacheConfig);
  }
  return factory.getClient(this.clientArgs);
}
