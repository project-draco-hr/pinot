{
  List<Object[]> propsWrapperList=new LinkedList<>();
  for (  Properties props : propList) {
    propsWrapperList.add(new Object[]{props});
  }
  return propsWrapperList.toArray(new Object[propsWrapperList.size()][]);
}
