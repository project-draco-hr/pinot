{
  try (Connection connection=getConnection()){
    T t=task.handle(connection);
    connection.commit();
    return t;
  }
 catch (  Exception e) {
    e.printStackTrace();
    return defaultReturnValue;
  }
}
