{
  if (args.length != 2) {
    System.err.println("Arguments must be configYml emailConfigPath");
    System.exit(1);
  }
  String configPath=args[0];
  String filePath=args[1];
  new TestAnomalyApplication(filePath,null,null,TestType.EMAIL).run(new String[]{"server",configPath});
}
