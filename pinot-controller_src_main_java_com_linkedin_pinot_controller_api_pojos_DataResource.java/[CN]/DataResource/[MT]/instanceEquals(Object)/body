{
  if (!(incoming instanceof DataResource)) {
    return false;
  }
  if (((DataResource)incoming).getResourceName().equals(resourceName) && (((DataResource)incoming).getNumberOfDataInstances() == numberOfDataInstances) && (((DataResource)incoming).getNumberOfCopies() == numberOfCopies)) {
    return true;
  }
  return false;
}
