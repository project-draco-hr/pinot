{
  resourceName=BrokerRequestUtils.getRealtimeResourceNameForResource(resourceName);
  return new RealtimeSegmentZKMetadata(propertyStore.get(constructPropertyStorePathForSegment(resourceName,segmentName),null,AccessOption.PERSISTENT));
}
