{
  return Objects.toStringHelper(DefaultThirdEyeClientConfig.class).add("expireAfterAccess",expireAfterAccess).add("expirationTime",expirationTime).add("expirationUnit",expirationUnit).toString();
}
