{
  if (!(o instanceof TimeSeriesRow)) {
    return false;
  }
  TimeSeriesRow ts=(TimeSeriesRow)o;
  return Objects.equals(start,ts.getStart()) && Objects.equals(end,ts.getEnd()) && Objects.equals(dimensionName,ts.getDimensionName())&& Objects.equals(dimensionValue,ts.getDimensionValue())&& Objects.equals(metrics,ts.getMetrics());
}
