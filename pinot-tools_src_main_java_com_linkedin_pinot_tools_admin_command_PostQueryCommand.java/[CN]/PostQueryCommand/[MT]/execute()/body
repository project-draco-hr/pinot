{
  if (_brokerHost == null) {
    _brokerHost=NetUtil.getHostAddress();
  }
  LOGGER.info("Executing command: " + toString());
  String result=run();
  LOGGER.info("Result: " + result);
  return true;
}
