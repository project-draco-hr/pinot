{
  AnomalyMergedResult mergedResult1=mergedResultDAO.findLatestByCollectionMetricDimensions(mergedResult.getCollection(),mergedResult.getMetric(),mergedResult.getDimensions());
  Assert.assertEquals(mergedResult1,mergedResult);
}
