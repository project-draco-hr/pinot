{
  String zkUrl="<ZKURL>/<CHROOT>(otional)/<CLUSTER_NAME>";
  Connection connection=ConnectionFactory.fromZookeeper(zkUrl);
  String tableName="<TABLE_NAME>";
  ResultSetGroup resultSetGroup=connection.execute(tableName,"select count(*) from " + tableName);
  System.out.println(resultSetGroup);
}
