{
  checkUpdateDefaultColumns(segmentDirectoryFile);
  SegmentPreProcessor processor=new SegmentPreProcessor(segmentDirectoryFile,indexLoadingConfigMetadata,newColumnsSchema3);
  processor.process();
}
