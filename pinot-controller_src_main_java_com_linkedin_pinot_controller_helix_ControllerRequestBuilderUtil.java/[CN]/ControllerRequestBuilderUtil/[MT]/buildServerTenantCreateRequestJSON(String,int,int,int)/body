{
  Tenant tenant=new TenantBuilder(tenantName).setType(TenantRole.SERVER).setTotalInstances(numberOfInstances).setOfflineInstances(offlineInstances).setRealtimeInstances(realtimeInstances).build();
  return tenant.toJSON();
}
