{
  if (fixedDimensions == null) {
    return true;
  }
  Collection<String> fixedValue=fixedDimensions.get(child.getDimensionName());
  return fixedValue == null || fixedValue.contains(StarTreeConstants.ALL) || fixedValue.contains(StarTreeConstants.STAR) || fixedValue.contains(child.getDimensionValue());
}
