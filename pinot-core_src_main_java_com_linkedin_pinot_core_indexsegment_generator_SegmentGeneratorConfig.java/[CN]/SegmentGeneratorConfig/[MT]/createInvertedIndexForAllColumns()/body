{
  if (schema == null) {
    throw new RuntimeException("schema cannot be null, make sure that schema is property set before calling this method");
  }
  createInvertedIndex=true;
  for (  FieldSpec spec : schema.getAllFieldSpecs()) {
    invertedIndexCreationColumns.add(spec.getName());
  }
}
