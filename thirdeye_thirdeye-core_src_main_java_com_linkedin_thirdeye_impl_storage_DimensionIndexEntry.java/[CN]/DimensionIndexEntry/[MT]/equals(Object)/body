{
  if (!(o instanceof DimensionIndexEntry)) {
    return false;
  }
  DimensionIndexEntry e=(DimensionIndexEntry)o;
  return nodeId.equals(e.getNodeId()) && fileId.equals(e.getFileId()) && dictionaryStartOffset.equals(e.getDictionaryStartOffset())&& dictionaryLength.equals(e.getDictionaryLength())&& bufferStartOffset.equals(e.getBufferStartOffset())&& bufferLength.equals(e.getBufferLength());
}
