{
  String schedule=dataDir.split("_")[1];
  String maxTime=dataDir.split("_")[3];
  return (schedule.equals(expireSchedule) && maxTime.compareTo(StarTreeConstants.DATE_TIME_FORMATTER.print(expireUptoDate)) <= 0);
}
