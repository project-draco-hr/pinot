{
  String betweenClause=pqlGenerator.getBetweenClause(DEFAULT_START,DEFAULT_END,DEFAULT_TIME_SPEC);
  Assert.assertEquals(betweenClause,"timeColumn BETWEEN '403224' AND '403247'");
  betweenClause=pqlGenerator.getBetweenClause(DEFAULT_START,DEFAULT_END,TEN_MINUTES_BUCKET_TIME_SPEC);
  Assert.assertEquals(betweenClause,"timeColumn BETWEEN '2419344' AND '2419487'");
}
