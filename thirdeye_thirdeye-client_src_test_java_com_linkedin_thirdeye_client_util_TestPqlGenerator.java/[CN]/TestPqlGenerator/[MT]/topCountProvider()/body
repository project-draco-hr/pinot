{
  int numTimeBuckets=(int)DEFAULT_TIME_SPEC.getBucket().getUnit().convert(DEFAULT_END.getMillis() - DEFAULT_START.getMillis(),TimeUnit.MILLISECONDS) + 1;
  int topCount=17;
  Set<String> dimensionGroup=Collections.singleton("dim1");
  return new Object[][]{new Object[]{topCount,numTimeBuckets,null,numTimeBuckets},new Object[]{topCount,numTimeBuckets,dimensionGroup,topCount * numTimeBuckets},new Object[]{null,numTimeBuckets,Collections.emptySet(),numTimeBuckets},new Object[]{null,numTimeBuckets,null,numTimeBuckets},new Object[]{null,numTimeBuckets,dimensionGroup,PqlGenerator.DEFAULT_TOP_COUNT_PER_BUCKET * numTimeBuckets}};
}
