{
  if (first == null) {
    return second;
  }
 else   if (second == null) {
    return first;
  }
  boolean isAnomaly=first.isAnomaly() || second.isAnomaly();
  return new AnomalyResult(isAnomaly);
}
