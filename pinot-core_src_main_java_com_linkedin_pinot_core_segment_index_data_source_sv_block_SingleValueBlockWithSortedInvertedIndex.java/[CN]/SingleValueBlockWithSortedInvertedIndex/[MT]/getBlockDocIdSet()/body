{
  if (p == null) {
    return BlockUtils.getDummyBlockDocIdSet(columnMetadata.getTotalDocs());
  }
  return BlockUtils.getBLockDocIdSetBackedByBitmap(DictionaryIdFilterUtils.filter2(p,invertedIndex,dictionary,columnMetadata));
}
