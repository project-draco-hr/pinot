{
  if (!(obj instanceof Expression)) {
    return false;
  }
  Expression other=(Expression)obj;
  return Objects.equals(other.singularValue,this.singularValue) && Objects.equals(other.arguments,this.arguments);
}
