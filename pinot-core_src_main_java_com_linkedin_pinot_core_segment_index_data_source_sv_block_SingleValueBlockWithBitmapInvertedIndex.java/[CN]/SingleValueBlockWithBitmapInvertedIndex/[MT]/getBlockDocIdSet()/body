{
  if (predicate == null) {
    return BlockUtils.getDummyBlockDocIdSet(columnMetadata.getTotalDocs());
  }
  final ImmutableRoaringBitmap orredBitmap=DictionaryIdFilterUtils.filter2(predicate,invertedIndex,dictionary,columnMetadata);
  return BlockUtils.getBLockDocIdSetBackedByBitmap(orredBitmap);
}
