{
  if (spec.getFieldType() == FieldType.dimension)   return getDimensionBlockValueSet();
 else   if (spec.getFieldType() == FieldType.metric)   return getMetricBlockValueSet();
  return getTimeBlockValueSet();
}
