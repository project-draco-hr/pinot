{
  addSchema(schemaFile,"schemaFile");
  Schema schema=Schema.fromFile(schemaFile);
  addOfflineTable("mytable","DaysSinceEpoch","daysSinceEpoch",3000,"DAYS",null,null,schema.getDimensionNames(),null);
}
