{
  return MoreObjects.toStringHelper(this).add("collection",collection).add("metricFunction",metricFunction).add("startTime",startTime).add("endTime",endTime).add("dimensionValues",dimensionValues).add("groupBy",groupBy).add("shouldGroupByTime",shouldGroupByTime).add("topCount",topCount).toString();
}
