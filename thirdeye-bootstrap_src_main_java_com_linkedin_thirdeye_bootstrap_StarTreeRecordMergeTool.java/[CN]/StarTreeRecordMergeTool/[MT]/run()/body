{
  try {
switch (fileType) {
case AVRO:
      runAvro();
    break;
default :
  throw new IllegalArgumentException("FileType " + fileType + " not supported");
}
}
 catch (Exception e) {
throw new RuntimeException(e);
}
}
