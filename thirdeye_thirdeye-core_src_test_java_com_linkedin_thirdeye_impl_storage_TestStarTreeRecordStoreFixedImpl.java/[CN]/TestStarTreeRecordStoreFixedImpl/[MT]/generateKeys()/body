{
  List<DimensionKey> keys=new ArrayList<DimensionKey>();
  for (int i=0; i < 10; i++) {
    DimensionKey key=new DimensionKey(new String[]{"A" + (i % 3),"B" + (i % 6),"C" + (i % 9)});
    keys.add(key);
  }
  return keys;
}
