{
  StarTreeQuery query=new StarTreeQueryImpl.Builder().setDimensionKey(getDimensionKey(StarTreeConstants.STAR,StarTreeConstants.STAR,StarTreeConstants.STAR)).setTimeRange(new TimeRange(0L,7L)).build(config);
  Number[] sums=recordStore.getMetricSums(query);
  Assert.assertEquals(sums,getSums(80));
}
