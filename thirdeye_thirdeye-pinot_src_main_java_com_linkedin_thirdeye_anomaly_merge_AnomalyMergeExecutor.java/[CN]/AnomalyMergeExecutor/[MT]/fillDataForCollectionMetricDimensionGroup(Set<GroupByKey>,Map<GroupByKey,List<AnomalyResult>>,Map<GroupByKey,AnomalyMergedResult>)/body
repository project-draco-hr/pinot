{
  List<GroupByRow<GroupByKey,Long>> groupByRows=anomalyResultDAO.getCountByCollectionMetricDimension(mergeConfig.getStartTime(),mergeConfig.getEndTime());
  for (  GroupByRow<GroupByKey,Long> row : groupByRows) {
    uniqueCategories.add(row.getGroupBy());
  }
}
