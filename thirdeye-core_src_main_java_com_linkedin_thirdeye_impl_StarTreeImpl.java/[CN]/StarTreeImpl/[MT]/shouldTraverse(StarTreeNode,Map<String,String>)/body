{
  if (fixedDimensions == null) {
    return true;
  }
  String fixedValue=fixedDimensions.get(child.getDimensionName());
  return fixedValue == null || fixedValue.equals(StarTreeConstants.STAR) || fixedValue.equals(child.getDimensionValue());
}
