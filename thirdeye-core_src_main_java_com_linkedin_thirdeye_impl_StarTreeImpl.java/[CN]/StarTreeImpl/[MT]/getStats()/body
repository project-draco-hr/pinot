{
  StarTreeStats stats=new StarTreeStats(config.getDimensionNames(),config.getMetricNames(),config.getTime().getColumnName(),config.getTime().getBucket().getSize(),config.getTime().getBucket().getUnit());
  getStats(root,stats);
  return stats;
}
