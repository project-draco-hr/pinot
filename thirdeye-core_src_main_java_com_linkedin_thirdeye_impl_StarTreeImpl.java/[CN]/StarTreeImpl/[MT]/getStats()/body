{
  StarTreeStats stats=new StarTreeStats(config.getDimensionNames(),config.getMetricNames(),config.getTimeColumnName(),config.getTimeBucketSize(),config.getTimeBucketSizeUnit());
  getStats(root,stats);
  return stats;
}
