{
  ServerInstance serverInstance=(ServerInstance)getApplication().getContext().getAttributes().get(ServerInstance.class.toString());
  dataManager=(InstanceDataManager)serverInstance.getInstanceDataManager();
}
