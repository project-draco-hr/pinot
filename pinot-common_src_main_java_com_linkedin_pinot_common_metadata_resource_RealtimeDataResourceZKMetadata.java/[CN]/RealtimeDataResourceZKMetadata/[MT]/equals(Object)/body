{
  if (isSameReference(this,anotherMetadata)) {
    return true;
  }
  if (isNullOrNotSameClass(this,anotherMetadata)) {
    return false;
  }
  RealtimeDataResourceZKMetadata metadata=(RealtimeDataResourceZKMetadata)anotherMetadata;
  return super.equals(metadata) && isEqual(_streamType,metadata._streamType) && isEqual(_dataSchema,metadata._dataSchema)&& isEqual(_streamMetadata,metadata._streamMetadata);
}
