{
  AbstractTableConfig config=AbstractTableConfig.init(entity.getText());
  manager.updateMetadataConfigFor(config.getTableName(),TableType.valueOf(config.getTableType()),config.getCustomConfigs());
  return new StringRepresentation("done");
}
