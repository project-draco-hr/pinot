{
  UploadSegmentCommand segmentUploader=new UploadSegmentCommand().setSegmentDir(_segmentDir.getAbsolutePath()).setControllerHost(_localhost).setControllerPort(_controllerPort);
  segmentUploader.execute();
  waitForExternalViewUpdate();
}
