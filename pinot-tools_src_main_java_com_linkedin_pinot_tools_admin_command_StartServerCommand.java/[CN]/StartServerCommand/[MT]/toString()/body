{
  return ("StartServerCommand -clusterName " + _clusterName + " -serverHost "+ _serverHost+ " -serverPort "+ _serverPort+ " -dataDir "+ _dataDir+ " -segmentDir "+ _segmentDir+ " -zkAddress "+ _zkAddress);
}
