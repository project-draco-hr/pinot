{
  if (metrics.isEmpty()) {
    throw new IllegalArgumentException("Must provide >= 1 metric for time series query");
  }
  return addDimensionValues(String.format("/timeSeries/raw/%s/%s/%d/%d",collection,COMMA_JOINER.join(metrics),startTime,endTime));
}
