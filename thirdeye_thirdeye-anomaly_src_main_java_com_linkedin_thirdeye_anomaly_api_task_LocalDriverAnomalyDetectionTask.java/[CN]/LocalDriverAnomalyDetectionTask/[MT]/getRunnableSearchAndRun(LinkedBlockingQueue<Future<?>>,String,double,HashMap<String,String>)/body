{
  return new Runnable(){
    @Override public void run(){
      try {
        searchAndRun(nextDimensionValues,nextGroupByDimension,nextProportionMultiplier,futures);
      }
 catch (      Exception e) {
        LOGGER.error("encountered exception in {}",getTaskInfo(),e);
      }
    }
  }
;
}
