{
  StringBuilder sb=new StringBuilder();
  sb.append("[");
  String delim="";
  for (  String name : names) {
    sb.append(delim).append(name).append(":").append(record.getDimensionValues().get(name));
    delim=",";
  }
  sb.append("]");
  return sb.toString();
}
