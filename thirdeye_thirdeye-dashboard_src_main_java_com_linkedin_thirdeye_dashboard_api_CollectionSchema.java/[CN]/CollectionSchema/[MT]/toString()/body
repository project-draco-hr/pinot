{
  return Objects.toStringHelper(CollectionSchema.class).add("dimensions",dimensions).add("dimensionAliases",dimensionAliases).add("metrics",metrics).add("metricAliases",metricAliases).toString();
}
