{
  if (!(o instanceof DimensionGroup)) {
    return false;
  }
  DimensionGroup g=(DimensionGroup)o;
  return Objects.equals(name,g.getName()) && Objects.equals(dimension,g.getDimension()) && Objects.equals(regex,g.getRegex())&& Objects.equals(values,g.getValues());
}
