{
  AnomalyFunctionSpec spec=new AnomalyFunctionSpec();
  spec.setType(type);
  if ("SCAN_STATISTICS".equals(type)) {
    spec.setProperties("pattern=UP;seasonal=168;minWindowLength=1;numSimulations=1000;monitoringWindow=72");
  }
  return spec;
}
