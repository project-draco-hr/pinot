{
  if (!super.equals(offlineDataResourceMetadata)) {
    return false;
  }
  if (!getPushFrequency().equals(offlineDataResourceMetadata.getPushFrequency()) || !getSegmentAssignmentStrategy().equals(offlineDataResourceMetadata.getSegmentAssignmentStrategy())) {
    return false;
  }
  return true;
}
