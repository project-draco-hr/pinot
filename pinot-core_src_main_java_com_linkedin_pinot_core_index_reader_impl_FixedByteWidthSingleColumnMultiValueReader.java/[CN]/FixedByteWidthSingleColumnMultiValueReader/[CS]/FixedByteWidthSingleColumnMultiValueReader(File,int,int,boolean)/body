{
  int headerSize=numDocs * SIZE_OF_INT * NUM_COLS_IN_HEADER;
  this.isMMap=isMMap;
  raf=new RandomAccessFile(file,"rw");
  if (isMMap) {
    headerSectionByteBuffer=MmapUtils.mmapFile(raf,FileChannel.MapMode.READ_ONLY,0,headerSize,file,this.getClass().getSimpleName() + " headerSectionByteBuffer");
  }
 else {
    headerSectionByteBuffer=MmapUtils.allocateDirectByteBuffer((int)headerSize,file,this.getClass().getSimpleName() + " headerSectionByteBuffer");
    raf.getChannel().read(headerSectionByteBuffer);
  }
  headerSectionReader=new FixedByteWidthRowColDataFileReader(headerSectionByteBuffer,numDocs,NUM_COLS_IN_HEADER,new int[]{SIZE_OF_INT,SIZE_OF_INT});
  int startIndex=headerSectionReader.getInt(numDocs - 1,0);
  int length=headerSectionReader.getInt(numDocs - 1,1);
  int totalNumValues=startIndex + length;
  int dataSize=totalNumValues * columnSizeInBytes;
  if (isMMap) {
    dataSectionByteBuffer=MmapUtils.mmapFile(raf,FileChannel.MapMode.READ_ONLY,headerSize,dataSize,file,this.getClass().getSimpleName() + " dataSectionByteBuffer");
  }
 else {
    dataSectionByteBuffer=MmapUtils.allocateDirectByteBuffer((int)dataSize,file,this.getClass().getSimpleName() + " dataSectionByteBuffer");
    raf.getChannel().read(dataSectionByteBuffer,headerSize);
    raf.close();
  }
  dataSectionReader=new FixedByteWidthRowColDataFileReader(dataSectionByteBuffer,totalNumValues,1,new int[]{columnSizeInBytes});
}
