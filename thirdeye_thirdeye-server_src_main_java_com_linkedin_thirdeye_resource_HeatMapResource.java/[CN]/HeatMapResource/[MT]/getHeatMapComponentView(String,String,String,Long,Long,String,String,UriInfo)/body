{
  return new HeatMapComponentView(getHeatMapComponentViewJson(type,collection,metric,startMillis,endMillis,aggregate,movingAverage,uriInfo),starTreeManager.getStarTree(collection).getConfig().getDimensions());
}
