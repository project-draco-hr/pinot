{
  if (matchingIds == null) {
    matchingIds=new int[matchingSize];
    int counter=0;
    for (int i=rangeStartIndex; i <= rangeEndIndex; i++) {
      matchingIds[counter++]=i;
    }
  }
  return matchingIds;
}
