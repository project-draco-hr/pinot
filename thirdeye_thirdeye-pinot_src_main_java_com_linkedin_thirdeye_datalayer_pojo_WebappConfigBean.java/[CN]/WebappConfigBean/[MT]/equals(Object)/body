{
  if (!(o instanceof WebappConfigBean)) {
    return false;
  }
  WebappConfigBean wc=(WebappConfigBean)o;
  return Objects.equals(getId(),wc.getId()) && Objects.equals(name,wc.getName()) && Objects.equals(collection,wc.getCollection())&& Objects.equals(type,wc.getType())&& Objects.equals(configMap,wc.getConfigMap());
}
