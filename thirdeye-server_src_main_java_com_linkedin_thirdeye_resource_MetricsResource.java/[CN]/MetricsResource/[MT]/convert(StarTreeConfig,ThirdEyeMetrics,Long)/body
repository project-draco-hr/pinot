{
  return new StarTreeRecordImpl(config,QueryUtils.convertDimensionMap(config.getDimensions(),metrics.getDimensionValues()),QueryUtils.convertMetricMap(config.getMetrics(),metrics.getMetricValues(),time));
}
