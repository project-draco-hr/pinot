{
  String cacheKey=serverUri + "/collections/" + URLEncoder.encode(collection,ENCODING)+ "/segments";
  List<SegmentDescriptor> descriptors=segments.get(cacheKey);
  if (descriptors.isEmpty()) {
    segments.invalidate(cacheKey);
  }
  return descriptors;
}
