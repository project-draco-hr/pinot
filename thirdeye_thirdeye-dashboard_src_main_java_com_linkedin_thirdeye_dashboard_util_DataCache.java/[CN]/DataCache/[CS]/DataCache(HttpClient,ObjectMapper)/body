{
  this.schemas=CacheBuilder.newBuilder().expireAfterWrite(5,TimeUnit.MINUTES).build(new CollectionSchemaCacheLoader(httpClient,objectMapper));
  this.collections=CacheBuilder.newBuilder().expireAfterWrite(5,TimeUnit.MINUTES).build(new CollectionsCacheLoader(httpClient,objectMapper));
}
