{
  if (getSecond() == 0) {
    return -1;
  }
  if (o.getSecond() == 0) {
    return 1;
  }
  if ((getFirst() / getSecond()) > (o.getFirst() / o.getSecond())) {
    return 1;
  }
  if ((getFirst() / getSecond()) < (o.getFirst() / o.getSecond())) {
    return -1;
  }
  return 0;
}
