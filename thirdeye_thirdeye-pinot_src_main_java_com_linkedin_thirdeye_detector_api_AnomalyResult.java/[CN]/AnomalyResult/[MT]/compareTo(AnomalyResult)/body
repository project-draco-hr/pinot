{
  int diff=ObjectUtils.compare(dimensions,o.getDimensions());
  if (diff != 0) {
    return diff;
  }
  diff=-ObjectUtils.compare(startTimeUtc,o.getStartTimeUtc());
  if (diff != 0) {
    return diff;
  }
  diff=ObjectUtils.compare(functionId,o.getFunctionId());
  if (diff != 0) {
    return diff;
  }
  return ObjectUtils.compare(id,o.getId());
}
