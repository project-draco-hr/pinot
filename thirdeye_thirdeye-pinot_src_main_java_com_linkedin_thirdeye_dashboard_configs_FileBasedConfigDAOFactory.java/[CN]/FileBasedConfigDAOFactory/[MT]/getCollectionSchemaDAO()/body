{
  final FileBasedConfigDAO<CollectionSchema> fileBasedConfigDAO=new FileBasedConfigDAO<>(rootDir,CollectionSchema.class.getSimpleName());
  collectionSchemaDAO=createDAO(fileBasedConfigDAO,CollectionSchema.class.getSimpleName());
  return collectionSchemaDAO;
}
