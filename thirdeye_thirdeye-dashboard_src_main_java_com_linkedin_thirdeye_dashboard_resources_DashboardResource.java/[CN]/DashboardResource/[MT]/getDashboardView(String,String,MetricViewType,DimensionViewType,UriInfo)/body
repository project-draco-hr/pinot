{
  long currentTime=System.currentTimeMillis();
  return Response.seeOther(URI.create(PATH_JOINER.join("","dashboard",collection,metricFunction,metricViewType,dimensionViewType,currentTime - DEFAULT_CURRENT_OFFSET - DEFAULT_BASELINE_DELTA,currentTime - DEFAULT_CURRENT_OFFSET))).build();
}
