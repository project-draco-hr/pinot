{
  return Objects.toStringHelper(HeatMap.class).add("metric",metric).add("metricAlias",metricAlias).add("dimension",dimension).add("dimensionAlias",dimensionAlias).add("statsNames",statsNames).toString();
}
