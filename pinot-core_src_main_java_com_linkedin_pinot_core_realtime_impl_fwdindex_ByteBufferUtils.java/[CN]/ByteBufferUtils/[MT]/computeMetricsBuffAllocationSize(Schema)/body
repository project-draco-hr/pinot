{
  int ret=0;
  for (  String metricName : schema.getMetrics()) {
switch (schema.getDataType(metricName)) {
case INT:
case FLOAT:
      ret+=4;
    break;
case LONG:
case DOUBLE:
  ret+=8;
break;
default :
break;
}
}
return ret;
}
