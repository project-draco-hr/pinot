{
  List<List<Integer>> list=new LinkedList<List<Integer>>();
  for (int i=0; i < 128; i++) {
    List<Integer> combination=ImmutableList.of(i % 2,i % 4,i % 8);
    list.add(combination);
  }
  return list;
}
