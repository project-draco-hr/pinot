{
  MetricTimeSeries ts=new MetricTimeSeries(metricSchema);
  ts.set(0,"M",1);
  StarTreeRecord record=new StarTreeRecordImpl.Builder().setDimensionKey(getDimensionKey("A0","B0","C0")).setMetricTimeSeries(ts).build(config);
  recordStore.update(record);
}
