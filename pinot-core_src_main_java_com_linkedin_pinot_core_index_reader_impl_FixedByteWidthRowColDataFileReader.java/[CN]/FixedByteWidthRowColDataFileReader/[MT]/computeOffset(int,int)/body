{
  if (row >= rows || col >= cols) {
    final String message=String.format("Input (%d,%d) is not with in expected range (%d,%d)",col,row,col,rows);
    throw new IndexOutOfBoundsException(message);
  }
  final int offset=row * rowSize + colOffSets[col];
  return offset;
}
