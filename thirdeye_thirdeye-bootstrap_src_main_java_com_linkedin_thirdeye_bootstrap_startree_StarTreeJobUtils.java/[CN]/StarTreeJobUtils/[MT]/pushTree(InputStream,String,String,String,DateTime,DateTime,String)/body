{
  String url=thirdEyeUri + "/collections/" + URLEncoder.encode(collection,ENCODING)+ "/starTree/"+ URLEncoder.encode(treeId,ENCODING)+ "/"+ minTime.getMillis()+ "/"+ maxTime.getMillis();
  if (schedule != null) {
    url+="?schedule=" + URLEncoder.encode(schedule,ENCODING);
  }
  return executeHttpPost(treeData,url);
}
