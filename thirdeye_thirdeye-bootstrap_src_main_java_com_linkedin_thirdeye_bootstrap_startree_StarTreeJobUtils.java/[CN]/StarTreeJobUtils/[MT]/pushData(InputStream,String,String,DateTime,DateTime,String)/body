{
  String url=thirdEyeUri + "/collections/" + URLEncoder.encode(collection,ENCODING)+ "/data/"+ minTime.getMillis()+ "/"+ maxTime.getMillis();
  if (schedule != null) {
    url+="?schedule=" + URLEncoder.encode(schedule,ENCODING);
  }
  LOG.info("POST {}",url);
  return executeHttpPost(leafData,url);
}
