{
  return Subject.doAs(loginContext.getSubject(),new PrivilegedAction<HttpResponse>(){
    @Override public HttpResponse run(){
      try {
        return httpClient.execute(httpHost,httpRequest);
      }
 catch (      IOException e) {
        throw new IllegalStateException(e);
      }
    }
  }
);
}
