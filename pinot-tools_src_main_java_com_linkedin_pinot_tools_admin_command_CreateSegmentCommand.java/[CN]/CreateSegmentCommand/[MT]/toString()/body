{
  return ("CreateSegment -schemaFile " + _schemaFile + " -dataDir "+ _dataDir+ " -format "+ _format+ " -readerConfigFile "+ _recordReaderConfigFile+ " -tableName "+ _tableName+ " -segmentName "+ _segmentName+ " -outDir "+ _outDir+ " -numThreads "+ _numThreads+ " -enableStarTreeIndex "+ _enableStarTreeIndex+ " -starTreeIndexSpecFile "+ _starTreeIndexSpecFile+ " -overwrite "+ _overwrite);
}
