{
  return ("CreateSegmentCommand -schemaFile " + _schemaFile + " -dataDir "+ _dataDir+ " -resourceName "+ _resourceName+ " -tableName "+ _tableName+ " -segmentName "+ _segmentName+ " -outDir "+ _outDir);
}
