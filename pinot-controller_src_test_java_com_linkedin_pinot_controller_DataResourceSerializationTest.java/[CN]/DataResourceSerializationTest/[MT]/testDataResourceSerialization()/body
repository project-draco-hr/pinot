{
  JSONObject dataResourceJson=ControllerRequestBuilderUtil.createOfflineClusterBrokerResourceUpdateConfig(1,"test").toJSON();
  ObjectMapper mapper=new ObjectMapper();
  DataResource resource=mapper.readValue(dataResourceJson.toString().getBytes(),DataResource.class);
}
