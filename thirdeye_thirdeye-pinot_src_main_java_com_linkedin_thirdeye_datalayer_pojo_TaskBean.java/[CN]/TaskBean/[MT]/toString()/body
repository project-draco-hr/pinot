{
  return MoreObjects.toStringHelper(this).add("id",getId()).add("status",status).add("startTime",taskStartTime).add("endTime",taskEndTime).add("taskInfo",taskInfo).add("lastModified",lastModified).toString();
}
