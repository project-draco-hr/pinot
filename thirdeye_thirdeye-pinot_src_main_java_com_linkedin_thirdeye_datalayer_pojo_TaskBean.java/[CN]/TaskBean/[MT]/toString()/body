{
  return MoreObjects.toStringHelper(this).add("id",getId()).add("status",status).add("startTime",startTime).add("endTime",endTime).add("taskInfo",taskInfo).add("lastModified",lastModified).toString();
}
