{
  if (!(o instanceof MetricIndexEntry)) {
    return false;
  }
  MetricIndexEntry e=(MetricIndexEntry)o;
  return nodeId.equals(e.getNodeId()) && fileId.equals(e.getFileId()) && startOffset.equals(e.getStartOffset())&& length.equals(e.getLength())&& timeRange.equals(e.getTimeRange());
}
