{
  StartServerCommand serverStarter=new StartServerCommand();
  serverStarter.setClusterName(_clusterName).setZkAddress(_zkAddress).setPort(_serverPort).setDataDir(TMP_DIR + "PinotServerData").setSegmentDir(TMP_DIR + "PinotServerSegment");
  serverStarter.execute();
}
