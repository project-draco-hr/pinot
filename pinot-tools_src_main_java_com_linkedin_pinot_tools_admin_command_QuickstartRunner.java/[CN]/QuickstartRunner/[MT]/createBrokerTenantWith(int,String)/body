{
  AddTenantCommand command=new AddTenantCommand().setControllerUrl("http://localhost:" + controllerPorts.get(0)).setName(tenantName).setInstances(number).setRole(TenantRole.BROKER).setExecute(true);
  command.execute();
}
