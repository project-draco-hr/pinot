{
  if (key == null || StringUtils.countMatches(key,SEPARATOR) != 1) {
    throw new IllegalArgumentException("Key cannot be null and must be of the form  " + "'prefix" + SEPARATOR + "collection' with exactly one '"+ SEPARATOR+ "'");
  }
  String[] split=key.split(SEPARATOR);
  return split;
}
