{
  StarTreeQuery query=new StarTreeQueryImpl.Builder().setDimensionValue("A","*").setDimensionValue("B","*").setDimensionValue("C","C0").build();
  Collection<StarTreeNode> nodes=starTree.findAll(query);
  Assert.assertEquals(nodes.size(),2);
}
