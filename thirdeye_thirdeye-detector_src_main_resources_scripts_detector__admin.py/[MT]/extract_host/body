def extract_host():
    if (len(sys.argv) > 1):
        host = sys.argv[1]
    else:
        host = hosts[0]
    if (host not in hosts):
        print 'WARNING: host "', host, '"is not officially supported'
    return host
