{
  DimensionKey searchKey=new DimensionKey(new String[]{"A0","B0","C0"});
  Map<DimensionKey,Integer> result=dimensionStore.findMatchingKeys(searchKey);
  Assert.assertEquals(result.size(),1);
  checkLogicalOffsets(result);
}
