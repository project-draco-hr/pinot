{
  return Objects.toStringHelper(ThirdEyeQuery.class).add("collection",collection).add("metricNames",metricNames).add("start",start).add("end",end).add("dimensionValues",dimensionValues).add("groupByColumns",groupByColumns).add("functions",functions).add("derivedMetrics",derivedMetrics).toString();
}
