{
  if (!(o instanceof StarTreeIndexSpec)) {
    return false;
  }
  StarTreeIndexSpec s=(StarTreeIndexSpec)o;
  return Objects.equal(maxLeafRecords,s.getMaxLeafRecords()) && Objects.equal(splitExcludes,s.getSplitExcludes()) && Objects.equal(excludedDimensions,s.getExcludedDimensions())&& Objects.equal(splitOrder,s.getSplitOrder());
}
