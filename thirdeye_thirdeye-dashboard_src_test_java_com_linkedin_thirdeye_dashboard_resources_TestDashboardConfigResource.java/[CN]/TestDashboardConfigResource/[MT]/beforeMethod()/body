{
  mockDataCache=mock(DataCache.class);
  mockCollectionSchema=mock(CollectionSchema.class);
  mockQueryCache=mock(QueryCache.class);
  when(mockDataCache.getCollectionSchema(SERVER_URI,COLLECTION)).thenReturn(mockCollectionSchema);
  testDashboardConfigResource=new DashboardConfigResource(SERVER_URI,mockDataCache,mockQueryCache,objectMapper);
}
